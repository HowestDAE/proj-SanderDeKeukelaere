<Page x:Class="Project_TF2ItemList.View.ItemInspection"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Project_TF2ItemList.View"
      xmlns:view="clr-namespace:Project_TF2ItemList.View"
      xmlns:convert="clr-namespace:Project_TF2ItemList.View.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="#282624"
      Title="ItemInspection">

    <Page.Resources>
        <FontFamily x:Key="TF2Font">pack://application:,,,/Resources/Fonts/#TF2 Build</FontFamily>
        <SolidColorBrush x:Key="TextColour" Color="#F1EFED"/>

        <convert:ClassToIconConverter x:Key="ClassToIcon"/>
        <convert:QualityToImageConverter x:Key="QualityToImage"/>
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Image HorizontalAlignment="Center"
               Grid.Row="1"/>

        <ScrollViewer Grid.Row="3" 
                 HorizontalContentAlignment="Center" 
                 Background="Transparent"
                 BorderBrush="Transparent">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock Text="GENUINE" 
                       Foreground="{StaticResource TextColour}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="20"
                       FontFamily="{StaticResource TF2Font}"
                       TextWrapping="Wrap"
                       Margin="0,3"
                       Grid.Row="0"/>

                <TextBlock Text="ROCKET LAUNCHER" 
                       Foreground="{StaticResource TextColour}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="20"
                       FontFamily="{StaticResource TF2Font}"
                       TextWrapping="Wrap"
                       Margin="0,3"
                       Grid.Row="1"/>

                <TextBlock Text="Minimal level: 0   Maximum level: 9000" 
                       Foreground="{StaticResource TextColour}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="20"
                       FontFamily="{StaticResource TF2Font}"
                       TextWrapping="Wrap"
                       Margin="0,30"
                       Grid.Row="2"/>

                <TextBlock Text="This is a description. This is a description. This is a description. This is a description. This is a description. This is a description. This is a description. This is a description." 
                       Foreground="{StaticResource TextColour}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="20"
                       FontFamily="{StaticResource TF2Font}"
                       TextWrapping="Wrap"
                       Width="700"
                       Margin="0,10"
                       Grid.Row="3"/>

                <ListView ItemsSource="{Binding Classes}"
                      HorizontalAlignment="Center"
					  VerticalAlignment="Center"
                      Background="Transparent"
                      BorderBrush="Transparent"
                      Foreground="{StaticResource TextColour}"
                      FontFamily="{StaticResource TF2Font}"
                      IsHitTestVisible="False"
                      Margin="0,20"
                      Grid.Row="4">

                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Converter={StaticResource ClassToIcon}}"
							   Width="45" Height="45"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Text="Item type: WEAPON" 
                       Foreground="{StaticResource TextColour}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="20"
                       FontFamily="{StaticResource TF2Font}"
                       TextWrapping="Wrap"
                       Margin="0,20"
                       Grid.Row="5"/>

                <TextBlock Text="Item Slot: PRIMARY" 
                       Foreground="{StaticResource TextColour}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="20"
                       FontFamily="{StaticResource TF2Font}"
                       TextWrapping="Wrap"
                       Margin="0,20"
                       Grid.Row="6"/>

                <TextBlock Text="CAPABILITIES" 
                       Foreground="{StaticResource TextColour}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"
                       FontFamily="{StaticResource TF2Font}"
                       TextWrapping="Wrap"
                       Margin="0,20,0,20"
                       Grid.Row="7"/>

                <ListView ItemsSource="{Binding Classes}"
                      HorizontalAlignment="Center"
					  VerticalAlignment="Center"
                      Background="Transparent"
                      BorderBrush="Transparent"
                      Foreground="{StaticResource TextColour}"
                      FontFamily="{StaticResource TF2Font}"
                      IsHitTestVisible="False"
                      Margin="0,10"
                      Grid.Row="8">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <Image Source="{Binding Checkmark}"
							   Width="45" Height="45"/>

                                <TextBlock Text="This item can do something" 
                                       Foreground="{StaticResource TextColour}"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"
                                       FontSize="20"
                                       FontFamily="{StaticResource TF2Font}"
                                       TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

        </ScrollViewer>
    </Grid>
</Page>
